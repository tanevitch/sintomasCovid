{% extends 'base.html.twig' %}

{% block title %}UserSintomas{% endblock %}

{% block body %}
    <h1>UserSintomas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_sintoma.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ user_sintoma.fecha ? user_sintoma.fecha|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ user_sintoma.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_sintomas_index') }}">back to list</a>

    <a href="{{ path('user_sintomas_edit', {'id': user_sintoma.id}) }}">edit</a>

    {{ include('user_sintomas/_delete_form.html.twig') }}
{% endblock %}
