{% extends 'base.html.twig' %}

{% block title %}Sintoma index{% endblock %}

{% block body %}
    <h1>Sintoma index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sintoma in sintomas %}
            <tr>
                <td>{{ sintoma.id }}</td>
                <td>{{ sintoma.descripcion }}</td>
                <td>
                    <a href="{{ path('sintoma_show', {'id': sintoma.id}) }}">show</a>
                    <a href="{{ path('sintoma_edit', {'id': sintoma.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('sintoma_new') }}">Create new</a>
{% endblock %}
